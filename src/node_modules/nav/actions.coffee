debug = require('debug')("cobudget-ui:nav:actions")

module.exports =
  navigate: (route) ->

    debug("dispatch navigate", route)

    if (typeof route == 'string')
      @dispatch("update", route)
    else
      @dispatch("route", route)
