extend = require('extend')
evaluate = require('./eval')

defaults = require('../../../config/defaults.coffee')
config = require(evaluate("../../../config/" + "test"))

module.exports = extend(config, defaults)
