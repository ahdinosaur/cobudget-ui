_ = require('lodash')

module.exports = {
  budgets:
    path: "/budgets"
    state: (flux) ->
      flux.store("Budgets").budgets
    #page: require('budgets/views/all.coffee')
  budget:
    path: "/budgets/:budgetId"
    state: (flux, params) ->
      budgets = flux.store("Budgets").budgets
      _.find(budgets, id: budgetId)
    page: require('budgets/views/overview.coffee')
}
