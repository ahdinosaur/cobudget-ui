# @cjsx React.DOM

debug = require('debug')("cobudget-ui:app:views:not-found")
React = require('react')
FluxChildMixin = require('fluxxor').FluxChildMixin(React)

module.exports = React.createClass
  mixins: [FluxChildMixin]

  render: ->
    debug("render'ing", @state, @props)

    return (
      <div>
        {"NOT FOUND! :("}
      </div>
    )
