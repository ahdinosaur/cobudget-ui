# @cjsx React.DOM

debug = require('debug')("cobudget-ui:app:views:index")
React = require('react')
FluxChildMixin = require('fluxxor').FluxChildMixin(React)

module.exports = React.createClass
  mixins: [FluxChildMixin]

  render: ->
    debug("render'ing", @state, @props)

    return (
      <div>
        {"hmm, this is the index."}
      </div>
    )
